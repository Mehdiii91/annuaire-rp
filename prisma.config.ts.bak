import { defineConfig } from '@prisma/config';

export default defineConfig({
  schema: './prisma/schema.prisma',
  dotenv: {
    path: '.env',
  },
  datasource: {
    db: {
      url: 'file:./dev.db',
    },
  },
});
